<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.EnvMapper">

    <select id="selectExistEnvId" resultType="java.lang.Integer">
        select id from env_info LIMIT 1
    </select>

    <select id="selectEnvIds" resultType="java.lang.Integer">
        select id from env_info
    </select>

</mapper>